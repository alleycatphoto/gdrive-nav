<?php
// shopify_styles.php - Contains all Shopify-specific styles
?>
/* Shopify Buy Button Customization */
.collection-header {
    margin-bottom: 2rem;
    padding: 0 1rem;
}

.collection-header h4 {
    color: var(--custom-icon);
    font-size: 1.25rem;
    font-weight: 500;
    margin: 0;
}

#collection-component-1736831470697 {
    margin: 0 -1rem;
}
.shopify-buy__product__variant-img {
    margin: 0 auto 15px auto;
    -webkit-transition: opacity 0.3s ease;
    transition: opacity 0.3s ease;
    opacity: 1;
    border-radius: .375rem;
}
.shopify-buy__product {
    transform: scale(0.75);
    transform-origin: top center;
    transition: transform 0.5s ease-in-out;
    background-color: #493849;
    border-radius: .375rem;
}
#collection-component-1736831470697 .shopify-buy__collection {
    margin: 0 !important;
    padding: 1rem 0 !important;
    bacground-color: var(--custom-bg);
    colo: var(--custom-text);
}

product-card {
    width: 100%;
    background-color: var(--custom-secondary);
    border: 1px solid var(--custom-icon);
    color: var(--custom-icon);
    border-radius: 0.25rem;
    transition: all 0.2s;
    text-decoration: none;
    display: inline-flex
;
    align-items: center;
    justify-content: center;
    gap: 0.5rem;
    margin-top: auto;
}
}
/* Rest of Shopify styles... */
    width: 100%;
    padding: 0.5rem 0.75rem;
    background-color: var(--custom-secondary);
    border: 1px solid var(--custom-icon);
    color: var(--custom-icon);
    border-radius: 0.25rem;
    transition: all 0.2s;
    text-decoration: none;
    display: inline-flex
;
    align-items: center;
    justify-content: center;
    gap: 0.5rem;
    margin-top: auto;